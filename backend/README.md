# VOLUMA backend

This directory will contain the FastAPI API, Alembic migrations, application services,
and Celery tasks from Phase 3 onward. The dependency manifest and lockfile are created
in Phase 0; no backend behavior exists yet.
